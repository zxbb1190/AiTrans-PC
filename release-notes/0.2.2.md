# AiTrans 0.2.2

## 中文说明
- 发布 `0.2.2` 正式稳定版，作为 GitHub Release 的稳定更新渠道版本。
- 保持 `0.2.1` 的当前桌面体验与功能，重点用于后续自动更新链路和正式分发。
- 优化版本发布一致性，确保安装包、`stable.yml` 与 release manifest 按同一版本号产出。

## English Notes
- Published `0.2.2` as the stable GitHub Release update channel version.
- Keeps the current desktop experience from `0.2.1`, mainly to support ongoing auto-update delivery and formal distribution.
- Improves release consistency so the installer, `stable.yml`, and the release manifest are generated under the same version.
